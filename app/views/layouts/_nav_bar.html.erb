<!-- Header Starts -->
<!-- <div class="top"> -->
  <header>
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <div class="logo">
            <h1><a href="/">Fly<span class="tblue">-n-</span>Ryde</a></h1>
          </div>
        </div>
        <div class="navigation pull-right">
          <% if current_user %>
            <%= link_to "Hello, #{current_user.first_name}!", user_path(current_user), :class=> "page-text" %>
            <%= link_to "Sign Out", signout_path, :class=> "page-text" %>
          <% else %>
            <%= link_to "Sign in with Uber", "/auth/Uber", :class=> "page-text" %>
          <% end %>
        </div>
      </div>
    </div>
  </header>
<!-- </div> -->
<!-- Header Ends -->

